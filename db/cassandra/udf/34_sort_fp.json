
{
  "type": "full-pushdown",
  "datasets": {
    "customer": {
      "table_schema": "customer",
      "index": "d_date_sk",
      "query": "SELECT c_birth_country, c_salutation, c_last_name, c_first_name, c_customer_id FROM tpc_ds.customer;"
    }
  },
  "steps": [
    {
      "etl_func": "nop",
      "args_tb": {
        "tb": "customer"
      },
      "args": {
        "nop": ""
      },
      "output": "customer",
      "remove": [
        "customer"
      ]
    }
  ]
}